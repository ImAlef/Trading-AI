[providers.python]
version = "3.9"

[build]
cmds = [
    "pip install --upgrade pip setuptools wheel",
    "pip install -r requirements.txt"
]

[start]
cmd = "py -3.9 main.py"

[variables]
PYTHONUNBUFFERED = "1"
PYTHONPATH = "."